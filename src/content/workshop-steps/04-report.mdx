---
title: "Report"
description: "Generate and email data-driven reports"
duration: "45 minutes"
difficulty: "intermediate"
objectives:
  - "Generate summaries with Gemini"
  - "Add a financial template"
  - "Send email (sandbox)"
---

import { CodeBlock } from '@/components/workshop/CodeBlock';
import { Exercise } from '@/components/workshop/Exercise';
import { Hint } from '@/components/workshop/Hint';
import { Checkpoint } from '@/components/workshop/Checkpoint';
import { TestButton } from '@/components/workshop/TestButton';



`ReportAgent` transforms analytics into readable reports using Gemini and can email results.

## Generate

Open the homepage and use the Report demo to generate a summary.

## TODO: Financial Template

<Exercise title="Create a financial report template">
Implement `generateFinancialReport` using a template with:
1. Executive Summary
2. Key Metrics
3. Trends Analysis
4. Recommendations

<CodeBlock language="tsx" code={`// Example call\nconst report = await reportAgent.generateFinancialReport({\n  revenue: 1234567.89,\n  topProducts: [{ name: 'UltraWidget', count: 542 }]\n});`} />

<Hint title="Working answer (click to reveal)">
<CodeBlock language="tsx" code={`async generateFinancialReport(data: any) {\n  const prompt = \`Using this financial data, write a structured report with:\n  1) Executive Summary\n  2) Key Metrics (bulleted)\n  3) Trends Analysis\n  4) Recommendations\n  Data: ${'${JSON.stringify(data, null, 2)}'}\n  Style: Professional, concise.\n  \`;\n  const res = await this.model.generateContent(prompt);\n  return res.response.text();\n}`} />
</Hint>

</Exercise>

## TODO: Email Delivery

<Exercise title="Enable email sending (sandbox safe)">
Uncomment the Gmail client call in `sendEmail`. Keep sandbox true for now.

<Hint title="Working answer (click to reveal)">
<CodeBlock language="tsx" code={`async sendEmail(recipient: string, subject: string, body: string) {\n  try {\n    // return await this.gmailClient.send({ recipient, subject, body });\n    return true;\n  } catch (e) {\n    console.error('Email send failed', e);\n    return false;\n  }\n}`} />
</Hint>

</Exercise>

<Checkpoint items={["Financial report renders 4 sections","Email preview shows in server logs"]} />

## Test

<TestButton label="Generate Sample Report" message="Use the Report demo on the homepage to generate and preview email." />


