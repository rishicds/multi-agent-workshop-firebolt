---
title: "Integration"
description: "Connect all agents into an end-to-end flow"
duration: "60 minutes"
difficulty: "intermediate"
objectives:
  - "Orchestrate multi-step flows"
  - "Handle errors and retries"
  - "Test end-to-end"
---

import { CodeBlock } from '@/components/workshop/CodeBlock';
import { Exercise } from '@/components/workshop/Exercise';
import { Hint } from '@/components/workshop/Hint';
import { Checkpoint } from '@/components/workshop/Checkpoint';
import { TestButton } from '@/components/workshop/TestButton';



Now connect orchestrator → analytics → report → email.

## Flow

<CodeBlock language="tsx" code={`// Pseudocode\nconst intent = await orchestrator.parseIntent(userQuery)\nif (intent.intent === 'multi_step') {\n  await orchestrator.handleMultiStepQuery(userQuery)\n}`} />

## Error Handling

Use retries for transient failures and show user-friendly messages.

<Hint title="Tip: Freemium resilience">
Use `withRetry` and the 15 RPM rate limiter from `error-handler.ts` when calling Gemini.
</Hint>

## Test

<TestButton label="End-to-end Prompt" message="Try in Live Demo: 'show revenue and send report to alice@example.com'" />

<Checkpoint items={["Multi-step prompt orchestrates all agents","User-friendly errors appear on failure"]} />


